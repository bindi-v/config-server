# this is the port on which our random-quote-service will run
server.port=2244

# allow for RefreshScope
#management.endpoints.web.exposure.include=*
management.endpoints.web.exposure.include=*

# the random-quote-service will use this as quote that it serves up
randomQuote=

